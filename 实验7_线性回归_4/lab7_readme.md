**实验内容：**
- 构造两类分类数据（类别 0 和类别 1）
- 对比无离群点和有离群点两种情况下的决策边界
- 分析离群点对 OLS 决策边界的影响
- 统计被误伤的样本数量及比例

**输出：**
- 终端打印决策边界位置、偏移量和误伤统计
- 可视化决策边界受离群点影响的散点图
- 保存基础数据和含离群点数据到 `.npy` 文件

---

## 注意事项

1. 代码已配置中文字体支持，避免绘图时出现乱码
2. 所有随机种子已固定为 42，确保实验可复现
3. 生成的 `.npy` 文件可用于后续分析和验证
**实验内容：**
- 使用 10 阶多项式特征拟合 sin(2πx) 函数
- 生成 50 个不同的训练数据集（每个 20 个样本）
- 测试 8 个不同的岭回归正则化参数 λ
- 计算并可视化偏差²、方差和总误差的权衡关系
- 对比过拟合、最优和欠拟合三种情况的拟合曲线

**输出：**
- 终端打印各 λ 值对应的偏差²、方差和总误差
- 偏差-方差权衡曲线图
- 三种极端情况的拟合曲线对比图
- 保存训练数据集到 `linear_regression_train_datasets.npy`

---

### 2. OLS 对离群点鲁棒性实验
bash 
python ols_robust.py
**实验内容：**
- 构造两类分类数据（类别 0 和类别 1）
- 对比无离群点和有离群点两种情况下的决策边界
- 分析离群点对 OLS 决策边界的影响
- 统计被误伤的样本数量及比例

**输出：**
- 终端打印决策边界位置、偏移量和误伤统计
- 可视化决策边界受离群点影响的散点图
- 保存基础数据和含离群点数据到 `.npy` 文件

---

## 注意事项

1. 代码已配置中文字体支持，避免绘图时出现乱码
2. 所有随机种子已固定为 42，确保实验可复现
3. 生成的 `.npy` 文件可用于后续分析和验证

# 机器学习上机实验07：线性回归-4

## 实验说明

本实验包含两个部分：
1. **偏差-方差权衡（Bias-Variance Trade-off）**：探究不同正则化参数对模型偏差、方差和总误差的影响
2. **线性回归对离群点的鲁棒性缺陷**：展示普通最小二乘法（OLS）在存在离群点时的决策边界偏移问题

## 环境依赖

确保已安装以下 Python 库：
- numpy
- matplotlib
- scikit-learn

## 运行命令

### 1. 偏差-方差权衡实验

